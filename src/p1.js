// Accepts a boolean value and returns the opposite value.
// not(true) would return false
// not(false) would return true
function not(input) {

}

// Accepts two boolean values and returns true if and only if both values are true
// and(true, false) => false
// and(true, true) => true
function and(a, b) {

}

// Accepts two boolean values and returns false if and only if both values are false
// or(false, false) => false
// or(true, false) => true
function or(a, b) {

}

module.exports = {
    not,
    and,
    or
};
