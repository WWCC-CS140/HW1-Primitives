// This function accepts an `input` string and returns a new string
// with only the first and last character of the the `input` string.
// The string will never be empty
// firstAndLastCharacter("JavaScript") => "Jt"
function firstAndLastCharacter(input) {

}

// This function accepts an `input` string and returns a new string
// with all spaces replaces with underscores, and all characters lowercase
// snakecase("JavaScript Rules!") => "javascript_rules!"
function snakecase(input) {

}

module.exports = {
    firstAndLastCharacter,
    snakecase
};
